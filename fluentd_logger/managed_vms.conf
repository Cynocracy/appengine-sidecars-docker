<source>
  @type multiprocess

  <process>
    cmdline -c /etc/google-fluentd/google-fluentd-child1.conf
    sleep_before_start 1s
    sleep_before_shutdown 5s
  </process>
  <process>
    cmdline -c /etc/google-fluentd/google-fluentd-child2.conf
    sleep_before_start 1s
    sleep_before_shutdown 5s
  </process>
</source>
